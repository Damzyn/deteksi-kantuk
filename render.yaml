services:
  - type: web
    name: deteksi-kantuk
    env: python
    buildCommand: |
      pip install -r requirements.txt
    startCommand: gunicorn --workers 1 --threads 4 --timeout 120 app:app
    envVars:
      - key: RENDER
        value: true
      - key: PYTHON_VERSION
        value: 3.10.12