[phases.setup]
nixPkgs = ["python310"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.start]
cmds = ["gunicorn --workers 1 --threads 4 --timeout 120 app:app"]

[build.env]
PYTHON_VERSION = "3.10.12"